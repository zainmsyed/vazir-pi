# Code Review 2026-05-26T12:16:11Z

**Status:** complete
**Created:** 2026-05-26T12:16:11Z
**Completed:** 2026-05-26
**Scope:** story
**Story:** story-026
**Static analysis:** fallow audit — pass (3 files scanned, no issues found)
**Focus:** story-026 completion review
**Trigger:** complete-story

---

## Goal
[Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.]

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete
---

## Findings
### Finding 1
- Severity: low
- Category: test-gap
- Summary: `runTurnEndIdempotencyScenario` tests idempotency through the legacy mini-consolidate candidates-file fallback rather than the modern JSON draft path.
- Evidence: `scripts/validate-vazir-complete-story.mts:1008` writes `story-001-candidates.md` (legacy format) instead of `story-001-learned-rule-closeout.json` (the JSON draft that production now uses). `finishLearnedRuleCloseout` in `.pi/extensions/vazir-context/complete-story.ts:705` only hits the legacy branch when the JSON draft is missing or invalid.
- Recommendation: Rewrite the idempotency test to write a valid `story-001-learned-rule-closeout.json` draft so it exercises the primary closeout path.
- Rule candidate: Regression tests should exercise the primary code path, not just the legacy fallback.

### Finding 2
- Severity: low
- Category: test-gap
- Summary: The idempotency test only covers the `learned-rule-closeout` phase; it does not verify repeated `turn_end` behavior during `readiness-review`, `review-in-progress`, or `review-closeout`.
- Evidence: Story-026 checklist item 1 calls for "repeated `turn_end` idempotency across complete-story closeout phases." `runTurnEndIdempotencyScenario` queues a single `learned-rule-closeout` path and emits `turn_end` three times. It does not assert idempotency for `readiness-review` (where the agent might queue duplicate readiness instructions) or `review-closeout` (where `markCompleteStoryReviewCloseoutReady` could be called repeatedly).
- Recommendation: Add lightweight idempotency assertions for at least one additional phase, such as `readiness-review`, to ensure repeated `turn_end` calls do not queue duplicate internal messages or mutate pending state.
- Rule candidate: When a checklist calls for coverage "across" a set of states, verify more than one representative state.

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] low — Rewrite `runTurnEndIdempotencyScenario` to use the JSON draft path instead of the legacy candidates-file fallback.
- [x] low — Add idempotency coverage for at least one additional closeout phase (e.g., `readiness-review` or `review-closeout`).

---

## Other Fixes
[Add any additional follow-up tasks here using `- [ ] severity — action` or simple `- [ ] description`. Mark items `[x]` only after the work is complete.]

---

## Completion Summary
Both recommended fixes are complete.

1. `runTurnEndIdempotencyScenario` now writes a valid `story-001-learned-rule-closeout.json` draft and exercises the primary JSON draft path through `agent_end`. The legacy candidates-file fallback is no longer used in this test.
2. Added `runReadinessReviewIdempotencyScenario` — emits `turn_end` three times while the story is in `readiness-review` phase and asserts that no duplicate internal messages are queued and the story remains open.

Both validation scripts (`validate-vazir-complete-story.mts` and `validate-vazir-review-loop.mts`) were re-run and pass successfully.
