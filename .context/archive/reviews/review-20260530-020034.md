# Code Review 2026-05-30T02:00:34Z

**Status:** complete
**Created:** 2026-05-30T02:00:34Z  
**Completed:** 2026-05-30
**Scope:** story  
**Story:** story-037  
**Static analysis:** fallow audit — pass (10 files scanned, no issues found)  
**Focus:** story-037 completion review  
**Trigger:** complete-story

---

## Goal
[Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.]

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete
---

## Findings
### Finding 1
- Severity: high
- Category: test-gap
- Summary: The story's validation coverage still asserts the abandoned inline-picker rollout, so the review target currently fails its own regression scripts after the revert to standard Pi selection lists.
- Evidence: Running `node --experimental-strip-types scripts/validate-vazir-story-plan-overlays.mts` now fails with `story should always show picker, then viewer, even when a story is active`, and `node --experimental-strip-types scripts/validate-vazir-implement-command.mts` fails with `implement should call ui.custom once when no story is active`. Those scripts still expect `ui.custom` picker usage and inline-picker behavior even though `.pi/extensions/vazir-tracker/index.ts` was reverted to `ui.select(...)` for `/story` and `/implement`.
- Recommendation: Update the validation scripts and any related harness assumptions to match the reverted product decision: standard Pi selection lists for choices, overlays only for opened markdown documents.
- Rule candidate: When reverting a UI rollout, update or remove the validation expectations for the abandoned interaction model in the same change so the targeted regression suite still describes the shipped behavior.

### Finding 2
- Severity: medium
- Category: dead-code
- Summary: `.pi/lib/vazir-ui.ts` still contains the custom inline selector implementation and comments from the abandoned style-C rollout even though no production command now uses it.
- Evidence: `showSelectionList()` in `.pi/lib/vazir-ui.ts` still renders the bespoke inline chrome (`▌`, inverted rows, custom keyboard loop, and "approved inline selector chrome" comment), while current production command code in `.pi/extensions/vazir-tracker/index.ts`, `.pi/extensions/vazir-context/index.ts`, and `.pi/extensions/vazir-context/complete-story.ts` has been reverted back to `ui.select(...)` for picker flows.
- Recommendation: Remove or replace the abandoned custom selector helper so shared UI code matches the current standard-picker decision and does not leave a misleading unused path behind.
- Rule candidate: After reverting a shared helper rollout, delete or reset the orphaned helper implementation so future callers cannot accidentally reintroduce the abandoned behavior.

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] high — Update story-037 regression scripts and harness expectations so they validate the reverted standard-picker behavior instead of the abandoned inline-picker rollout.
- [x] medium — Remove or reset the unused custom `showSelectionList()` implementation in `.pi/lib/vazir-ui.ts` so shared UI code matches the current product decision.

---

## Other Fixes
- [ ] medium — Reconcile story-035/story-036 helper wording with the actual shared-helper surface once the selector helper cleanup lands.

---

## Completion Summary
Completed all recommended-fix items from this review. The story-037 regression scripts now match the reverted standard-picker behavior and pass again (`scripts/validate-vazir-ui-helpers.mts`, `scripts/validate-vazir-story-plan-overlays.mts`, and `scripts/validate-vazir-implement-command.mts`), and `.pi/lib/vazir-ui.ts` has been reset away from the abandoned custom inline selector path back to the overlay-based `SelectList` helper. The only remaining follow-up is the optional unchecked item in `## Other Fixes` to reconcile older story wording with the final helper surface.
