# Code Review 2026-05-28T12:23:40Z

**Status:** complete
**Created:** 2026-05-28T12:23:40Z  
**Completed:** 2026-05-28
**Scope:** story  
**Story:** story-034  
**Static analysis:** fallow audit — pass (7 files scanned, no issues found)  
**Focus:** story-034 completion review  
**Trigger:** complete-story

---

## Goal
Inspect story-034 implementation for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

---

## Findings

### Finding 1
- **Severity:** low
- **Category:** dead-code
- **Summary:** `planWriteStep` and `storyWriteStep` string variables are computed but never consumed after the instruction was simplified.
- **Evidence:** `.pi/extensions/vazir-context/index.ts` lines 1757–1762 define both variables; `grep` confirms zero references elsewhere in the file.
- **Recommendation:** Remove the two unused `const` declarations.
- **Rule candidate:** After simplifying an instruction or prompt, scan for orphaned variable declarations that were only consumed by the old text.

### Finding 2
- **Severity:** low
- **Category:** test-gap
- **Summary:** No validation scenario asserts that ordinary non-command user input passes through the `input` handler unchanged after the plan-pending transformation was removed.
- **Evidence:** `scripts/validate-vazir-plan-questions.mts` tests instruction conciseness and replan behavior, but does not emit an `input` event and assert `{ action: "continue" }`.
- **Recommendation:** Add a lightweight scenario that emits a generic user message through the `input` handler and asserts it returns `{ action: "continue" }`.
- **Rule candidate:** When removing an input-transformation feature, add a regression test proving the default passthrough path still works.

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] low — Remove unused `planWriteStep` and `storyWriteStep` variables from `.pi/extensions/vazir-context/index.ts`
- [x] low — Add input-handler passthrough regression test to `scripts/validate-vazir-plan-questions.mts`

---

## Other Fixes
- [x] No additional follow-up tasks.

---

## Completion Summary
Reviewed story-034 implementation: the `plan-pending.json` mechanism was fully removed, the `/plan` instruction was simplified from ~50 lines to ~21 lines, and validation scripts were updated. Both findings have been remediated:

1. **Dead code fixed** — Removed unused `planWriteStep` and `storyWriteStep` variable declarations from `.pi/extensions/vazir-context/index.ts`.
2. **Test gap fixed** — Added `runInputPassthroughScenario` to `scripts/validate-vazir-plan-questions.mts`, which emits a normal user message through the `input` handler and asserts `{ action: "continue" }`. All validation scenarios pass.

No unresolved items remain. No bugs, regressions, or workflow violations were found in the scoped changes.
