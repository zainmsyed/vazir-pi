# Intake Brief

**Last updated:** 2026-06-06

## Planning brief
Replan around malformed story files generated by `/plan`. Vazir must validate generated stories immediately after planning, detect invalid story status values and malformed checklist/task formatting, and automatically run an internal repair loop that fixes only the malformed story files before the user sees the plan. This should be silent on success, retry a bounded number of times, and hard-fail only if the repair loop cannot produce valid stories. Also add a defensive validation gate before `/implement` so later manual drift cannot break core workflows. Keep the scope focused on story-file validation, repair-loop orchestration, and regression coverage for malformed `/plan` output and `/implement` fallback validation.

## Source files
- .context/intake/prd/Vazir_POC_Spec_v4_1_Addendum_E.md (12100 bytes)

## Distilled notes
### .context/intake/prd/Vazir_POC_Spec_v4_1_Addendum_E.md
- Addendum E keeps Vazir's command semantics and `.context/` contracts stable while improving TUI behavior using shared helpers and tracker-owned chrome.
- Existing architecture favors centralized workflow helpers and native pi TUI primitives over ad-hoc custom rendering.
- Width-safe rendering is already a documented concern in the shared UI layer.

### Clarified planning answers
- Malformed stories should be repaired in place by Vazir's internal loop, not duplicated or surfaced to the user during the happy path.
- The repair loop should stay silent on success and only fail visibly if bounded retries cannot produce valid story files.
- `/implement` still needs a defensive validation gate so later manual drift cannot break core workflows.

## Planning rules
- Treat listed source files as user-authored planning inputs unless they are explicitly marked as generated artifacts.
- Vazir-generated files in .context/stories/ are replan context, not primary intake.
- Read all text-based planning sources before asking questions.
- Ask only implementation-blocking delta questions after reviewing this brief and any raw files you actually need.
- State safe default assumptions briefly so the user can correct them.
- Surface contradictions instead of resolving them silently.
