# Code Review 2026-07-24T00:14:06Z

**Status:** complete
**Created:** 2026-07-24T00:14:06Z  
**Completed:** 2026-07-24
**Scope:** story  
**Story:** story-059  
**Static analysis:** not run (fallow unavailable)  
**Focus:** story-059 completion review  
**Trigger:** complete-story

---

## Goal
[Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.]

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete
---

## Findings
### Finding 1
- Severity: medium
- Category: regression
- Summary: `/plan` instruction no longer explicitly listed several formatting invariants that `validateStoryFile()` enforces.
- Evidence: The original instruction enumerated frontmatter trailing-space rules (`'**Label:** value' with exactly two trailing spaces`), the exact heading format (`'# Story NNN: Title'`), and the required sections (`Goal, Verification, Scope, Out of scope, Dependencies, Checklist, Issues, Completion Summary`). The revised instruction collapsed these into a single line that only mentioned status values, checklist item syntax, and checklist item count. The embedded template showed the format, but agents frequently deviate from templates unless each invariant is stated explicitly.
- Recommendation: Restore explicit formatting rules in the `/plan` instruction, either as a compact bulleted list or as a concise paragraph, covering: frontmatter line syntax and trailing spaces, exact heading, required sections, status values, and checklist item syntax/count. This preserves the hard-won story-058 invariant that `/plan` should not emit malformed stories.
- Rule candidate: `/plan` instructions must enumerate every formatting invariant that downstream story-file validators enforce.

### Finding 2
- Severity: low
- Category: test-gap
- Summary: The new regression coverage validated the instruction text but did not prove the `/plan` → file-write transition end-to-end.
- Evidence: `scripts/validate-vazir-plan-repair.mts` originally asserted only that the instruction contains boundary phrases and directives. A live `pi --print` smoke test was performed manually in `/tmp/vazir-story059-smoke-frGnwq` and succeeded, but no automated test reproduced that behavior.
- Recommendation: Strengthen the automated test to assert that the instruction explicitly names the `write tool`, `plan.md`, and `intake-brief.md`, and document the manual smoke-test command in the story completion notes.
- Rule candidate: —

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] medium — Restore explicit story-formatting rules (frontmatter trailing spaces, exact heading, required sections) in the `/plan` instruction.
- [x] low — Strengthen the automated test to assert the instruction names the write tool, `plan.md`, and `intake-brief.md`; document the manual smoke-test command in the story notes.

---

## Other Fixes
[Add any additional follow-up tasks here using `- [ ] severity — action` or simple `- [ ] description`. Mark items [x] only after the work is complete.]

---

## Completion Summary
Both review findings have been remediated and verified.

1. **Formatting rules restored** — `.pi/extensions/vazir-context/index.ts` now includes the full formatting-rule list again (frontmatter trailing spaces, exact heading, required sections, status values, checklist syntax/count). The `/plan` instruction is 62 lines, still under the 65-line conciseness ceiling in `validate-vazir-plan-questions.mts`.

2. **Test coverage strengthened** — `scripts/validate-vazir-plan-repair.mts` Test 6 now asserts that the instruction explicitly names the `write tool`, `.context/stories/plan.md`, and `.context/stories/intake-brief.md`, and preserves the frontmatter, heading, and required-sections rules. The manual smoke-test command was documented in `.context/stories/story-059.md`.

All targeted validations pass: `validate-vazir-plan-repair.mts`, `validate-vazir-plan-questions.mts`, `validate-vazir-story-file-validation.mts`. No bugs, dead code, simplification opportunities, scope drift, or workflow violations remain. The review is complete and the story may proceed to closeout.
