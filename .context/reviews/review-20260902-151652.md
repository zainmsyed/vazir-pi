# Code Review 2026-09-02T15:16:52Z

**Status:** complete  
**Created:** 2026-09-02T15:16:52Z  
**Completed:** 2026-09-02  
**Scope:** story  
**Story:** story-082  
**Focus:** story-082 completion review  
**Trigger:** complete-story

---

## Goal
Inspect story-082 (Remove Fallow from Vazir reviews and closeout) and its direct integration points for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

---

## Findings

### Finding 1
- Severity: medium
- Category: dead-code
- Summary: Five orphaned Fallow validation scripts remain on disk after story-082 removed them from the aggregate runner; at least one imports exports that no longer exist and would crash if run.
- Evidence: `scripts/validate-vazir-fallow-recurrence.mts` (lines 4-8) imports `reviewFallowFindingsFromFile` and `appendFallowToComplaintsLog` from `.pi/extensions/vazir-context/helpers.ts`, where story-082 deleted both functions. `scripts/run-validations.mts` no longer lists any `validate-vazir-fallow-*.mts`, so `npm test` is green while the files sit in `scripts/` looking runnable.
- Recommendation: Delete the five `scripts/validate-vazir-fallow-*.mts` files (or move them under a `scripts/archive/` location that the runner never scans) in an approved follow-up. Do not re-add them to the aggregate runner.
- Rule candidate: When removing a feature, remove its dedicated validations in the same change; dropping them only from the aggregate runner leaves broken, runnable-looking tests behind.

### Finding 2
- Severity: medium
- Category: workflow
- Summary: `.context/` is fossil-ignored via a duplicate tail block in `.fossil-settings/ignore-glob`, so story-082.md, story-083.md, and new review files are unversioned — the closeout commit would silently omit the project brain.
- Evidence: `fossil extras` reports no untracked `.context` files; `fossil ls .context/stories/` stops at story-068; the appended defaults block in `.fossil-settings/ignore-glob` lists `.context/`. The block is committed (no working-dir diff), so it predates this story, but it directly blocks this story's closeout artifacts from being committed.
- Recommendation: Remove the `.context/` line from the appended defaults (a protected VCS action that requires explicit user approval), then `fossil add` story-082.md, story-083.md, and the new review files. Follow-up: make `ensureFossilIgnoreGlob` in the init path refuse to ignore `.context/` so init can never re-introduce this.
- Rule candidate: —

### Finding 3
- Severity: low
- Category: workflow
- Summary: This review file was generated by stale in-session extension code: it carried `**Static analysis:** not run (fallow unavailable)` frontmatter and a Fallow-era requirements list, artifacts story-082 removed from the templates.
- Evidence: The generated frontmatter and the 15-requirement review prompt (including the Fallow Findings preservation rule) do not match current code: `helpers.ts` `REVIEW_FRONTMATTER_ORDER` no longer contains "Static analysis", a fresh `createReviewDraft` call produces no Fallow content, and current `buildReviewInstruction` emits 14 requirements with no Fallow mention. The running session's extension predates the story-082 edits; live-reload did not refresh it before /complete-story ran.
- Recommendation: Stale frontmatter line removed from this file while writing the review; the `## Fallow Findings` section is preserved per the review requirements. Follow-up: verify `vazir-live-reload` actually fires in long-running sessions, or restart Pi after large extension refactors before triggering closeout flows.
- Remediation (2026-09-02): root cause confirmed in `.pi/extensions/vazir-live-reload.ts` — `fs.watch` is non-recursive on Linux and the directory snapshot was flat, so edits inside `.pi/extensions/vazir-context/` were invisible to the watcher. Fixed: snapshot now walks recursively and one watcher is attached per extension subdirectory. Verified with the manual `validate-vazir-live-reload.mts` check, which now reports `Reloading Pi after extension change: vazir-context/index.ts (change)`.
- Rule candidate: After large extension refactors, confirm the running session has reloaded the code before triggering flows that generate files from templates.

### Finding 4
- Severity: low
- Category: workflow
- Summary: story-082.md's Scope section was never updated to include `scripts/validate-vazir-package-install.mts`, even though the file was modified under the approved closeout scope expansion.
- Evidence: `fossil status` shows `scripts/validate-vazir-package-install.mts` as EDITED; the story Scope section lists nine files but not this one. The expansion is documented only in the Issues section (line 52).
- Recommendation: Add the file to the story Scope section before the story is archived.
- Rule candidate: —

### Finding 5
- Severity: low
- Category: dead-code
- Summary: `validate-vazir-init.mts` retains fake npm-install scaffolding that no scenario uses after the Fallow install scenario was removed.
- Evidence: The `fakeNpmInstallForCwd` tool-path option and the `npm-install-args.txt` logger (lines ~26, 100-114) have no remaining callers in the file; the only consumer was `runFallowInstallScenario`, deleted earlier in this story.
- Recommendation: Remove the unused scaffolding in a follow-up cleanup.
- Rule candidate: —

### Finding 6
- Severity: low
- Category: simplification
- Summary: `buildReviewInstruction` keeps an unused `_unusedPrompt` second parameter "for caller compatibility" even though every caller now passes an empty string and the Fallow prompt it carried is gone.
- Evidence: `helpers.ts` signature `buildReviewInstruction(review, _unusedPrompt = "", cwd = "")`; the only caller, `startReviewFlow` in `index.ts`, passes `""`.
- Recommendation: Drop the parameter and update the harness type in `scripts/validate-vazir-review-design-compliance.mts` in a follow-up.
- Rule candidate: —

### Finding 7
- Severity: low
- Category: test-gap
- Summary: No active validation guards that legacy review files containing Fallow sections still validate and repair — historical-data preservation is a stated story verification criterion but has no regression test.
- Evidence: `validate-vazir-review-loop.mts` asserts new files are LLM-only (no "Static analysis"/"Fallow") but never feeds a Fallow-era file through `validateReviewDocument`/`repairReviewDocument`. Manual verification in this review (a legacy fixture with `**Static analysis:**` frontmatter and a `## Fallow Findings` section) validated `true` against current helpers, confirming the behavior works — it is just untested.
- Recommendation: Add a legacy-fixture case to `validate-vazir-review-loop.mts` asserting historical Fallow-bearing reviews still validate and repair without data loss.
- Rule candidate: —

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] medium — Delete or archive the five orphaned `scripts/validate-vazir-fallow-*.mts` files (deleted; no remaining references; aggregate suite green)
- [x] medium — Remove the `.context/` entry from `.fossil-settings/ignore-glob` (approved via `VCS_APPROVE vcs-xgnh29`; `.context/` line removed from the appended defaults, `.fallow/` preserved; story-082.md, story-083.md, review-20260902-151652.md, story-082-complete-story-closeout.json, and idea-007.md staged with `fossil add`)
- [x] low — Verify `vazir-live-reload` fires in long sessions (or restart Pi after large refactors) before closeout flows (root-caused and fixed: recursive snapshot + per-subdirectory watchers; manual live-reload validation passes and now detects nested edits)
- [x] low — Add `scripts/validate-vazir-package-install.mts` to the story-082 Scope section
- [x] low — Remove unused fake-npm scaffolding from `scripts/validate-vazir-init.mts`
- [x] low — Drop the unused `_unusedPrompt` parameter from `buildReviewInstruction` (signature, sole caller, and harness type updated)
- [x] low — Add a legacy Fallow-era review fixture case to `scripts/validate-vazir-review-loop.mts` (added; it exposed a real repair-path data-loss bug — `ensureReviewSectionsInOrder` dropped non-standard sections — fixed in helpers.ts, test green)

---

## Other Fixes
- None.

---

## Completion Summary
Reviewed the full story-082 working diff: `vazir-context` extension files (`index.ts`, `helpers.ts`, `complete-story.ts`), six active validation scripts, the aggregate runner, and the story/plan context files. The core Fallow removal is sound: the audit bridge, Fallow prompt injection, static-analysis frontmatter, Fallow Findings template section, complaints-log Fallow pipeline, and closeout consumption are all gone with no dangling references, and a real Pi smoke session generated an LLM-only review file.

**Remediation pass (2026-09-02) — all seven recommended fixes are complete and verified.** The five orphaned `validate-vazir-fallow-*.mts` files were deleted (no remaining references). The live-reload staleness from Finding 3 was root-caused to a real bug — non-recursive `fs.watch` plus a flat snapshot meant nested extension edits were never detected — and fixed in `vazir-live-reload.ts` (recursive snapshot, per-subdirectory watchers), verified by the manual live-reload validation now detecting `vazir-context/index.ts` changes. `validate-vazir-package-install.mts` was added to the story Scope section. Dead fake-npm scaffolding was removed from `validate-vazir-init.mts`. The unused `_unusedPrompt` parameter was dropped from `buildReviewInstruction` (signature, caller, harness type). The new legacy Fallow-era fixture in `validate-vazir-review-loop.mts` exposed a genuine repair-path data-loss bug — `ensureReviewSectionsInOrder` silently dropped non-standard sections such as historical `## Fallow Findings` — fixed in `helpers.ts` so repair now appends unknown sections instead of deleting them. Finally, the `.context/` line was removed from `.fossil-settings/ignore-glob` under explicit VCS approval (`VCS_APPROVE vcs-xgnh29`) and the branch's story/review/idea artifacts were staged with `fossil add`.

Verification after remediation: targeted validations (review-loop, init, review-design-compliance, complete-story, manual live-reload check) all pass, and the aggregate suite is green 41/41 in the Herdr test tab (log: `/home/zain/Documents/coding/test/vazir-082-remediation-aggregate.log`).

Residual notes for follow-up stories (not review blockers): the wider backlog of untracked `.context/` files (stories 069-081, older reviews, archive contents) is now visible to fossil but deliberately left unstaged — mass-versioning them is a separate user decision. A future story should make `ensureFossilIgnoreGlob` refuse to ignore `.context/` so init can never re-introduce the project-brain ignore.

Story status was not changed; completion remains the user's explicit call.
