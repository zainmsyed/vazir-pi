# Code Review 2026-05-18T22:02:11Z

**Status:** complete  
**Created:** 2026-05-18T22:02:11Z  
**Completed:** 2026-05-18  
**Scope:** story  
**Story:** story-020  
**Static analysis:** fallow audit — pass (18 files scanned, no issues found)  
**Focus:** story-020 completion review  
**Trigger:** complete-story

---

## Goal
[Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.]

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete
---

## Findings
### Finding 1
- Severity: high
- Category: regression
- Summary: The review-helper extraction broke multiple review validation scripts, so story-020 currently regresses its own verification surface.
- Evidence: `scripts/validate-vazir-review-design-compliance.mts` still loads `.pi/extensions/vazir-context/helpers.ts` and then calls `helpers.reviewFileTemplate(...)` / `helpers.createReviewDraft(...)` / `helpers.buildReviewInstruction(...)` even though those exports now live in `.pi/extensions/vazir-review/helpers.ts`; running `node scripts/validate-vazir-review-design-compliance.mts` now fails with `TypeError: helpers.reviewFileTemplate is not a function`. `scripts/validate-vazir-fallow-signal-sources.mts` and `scripts/validate-vazir-fallow-recurrence.mts` still import the moved review helpers from `../.pi/extensions/vazir-context/helpers.ts`; both now fail immediately with `SyntaxError: ... does not provide an export named ...`. By contrast, `node scripts/validate-vazir-complete-story.mts` and `node scripts/validate-vazir-split-scaffolding.mts` pass, which isolates the breakage to stale direct integration points around the extracted review helpers.
- Recommendation: Repoint the broken validation scripts to `.pi/extensions/vazir-review/helpers.ts` (or to a stable shared facade if one is intended), then rerun the affected scripts so story-020 again preserves working review validation coverage after the extraction.
- Rule candidate: When extracting helpers into a new module, update every validation/import site that exercises those helpers and rerun the moved module's validation scripts before closing the story.

---

## Fallow Findings
- No Fallow findings.

---

## Recommended Fixes
- [x] high — Repoint the broken review validation scripts (`validate-vazir-review-design-compliance`, `validate-vazir-fallow-signal-sources`, `validate-vazir-fallow-recurrence`) to the extracted `vazir-review` helpers and rerun them.

---

## Other Fixes
[Add any additional follow-up tasks here using `- [ ] severity — action` or simple `- [ ] description`. Mark items [x] only after the work is complete.]

---

## Completion Summary
The high-severity regression has been fixed. `validate-vazir-review-design-compliance.mts` now loads its review helpers from `vazir-review/helpers.ts`. `validate-vazir-fallow-signal-sources.mts` now splits imports between `vazir-context/helpers.ts` (for consolidation/closeout helpers) and `vazir-review/helpers.ts` (for review-specific helpers). `validate-vazir-fallow-recurrence.mts` now imports from `vazir-review/helpers.ts`. All three scripts pass: `node scripts/validate-vazir-review-design-compliance.mts`, `node scripts/validate-vazir-fallow-signal-sources.mts`, and `node scripts/validate-vazir-fallow-recurrence.mts`.